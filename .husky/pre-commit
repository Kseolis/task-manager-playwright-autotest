#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit hooks
# 
# ЗАЧЕМ: Автоматическая проверка кода перед коммитом
# ПОЧЕМУ: Предотвращаем коммит плохого кода в репозиторий
# 
# АЛЬТЕРНАТИВЫ:
# 1. GitHub Actions - проверка после push (но поздно)
# 2. GitLab CI - проверка в pipeline (но поздно)
# 3. IDE plugins - проверка в редакторе (но не все используют)
# 
# ВЫБОР: Husky + lint-staged - проверка ДО коммита, быстро, локально
#        Это стандарт индустрии для Node.js проектов

# Запускаем lint только для измененных файлов
npx lint-staged

