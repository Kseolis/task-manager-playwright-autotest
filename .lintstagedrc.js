/**
 * lint-staged конфигурация
 *
 * ЗАЧЕМ: Запускаем проверки только для измененных файлов
 * ПОЧЕМУ: Быстрее чем проверять весь проект
 *
 * АЛЬТЕРНАТИВЫ:
 * 1. Проверять весь проект - медленно для больших проектов
 * 2. Ручная проверка - легко забыть
 *
 * ВЫБОР: lint-staged - автоматически, быстро, только измененные файлы
 */
export default {
  '*.{js,jsx}': [
    'eslint --fix', // Автоматически исправляем что можем
    'prettier --write', // Форматируем код (если используется)
  ],
  '*.{json,md}': [
    'prettier --write', // Форматируем JSON и Markdown
  ],
}
